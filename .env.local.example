# Reply card. When set at build time, /contact renders a real form that
# POSTs here (Formspree, Web3Forms, or any endpoint accepting FormData).
# When unset, the contact page renders the engraved address block with a
# mailto: link instead. Static export means there is no server route.
NEXT_PUBLIC_FORM_ENDPOINT=
